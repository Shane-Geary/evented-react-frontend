const defaultState = {
    loadingState: "inactive",
    arr: []
}


export default function groupsReducer(state = defaultState, action) {
    switch(action.type) {
        case
    }
    return state 
}